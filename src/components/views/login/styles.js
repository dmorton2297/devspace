const styles = (theme) => {
    return ({
        container: {
            backgroundColor: theme.palette.primary,
            height: '100%',
            width: '100%',
            alignItems: 'center',
            paddingTop: 100
        },
        loginControl: {
            width: 400,
            height: 300
        }
    });
};

export default styles;