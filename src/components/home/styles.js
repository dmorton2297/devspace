const styles = (theme) => {
    return ({
        container: {
            backgroundColor: theme.palette.primary.light,
            width: 200,
            height: 200
        }
    });
};

export default styles;
